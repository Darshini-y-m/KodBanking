# AIVEN PostgreSQL - Use DB_URL (full connection string) OR individual vars
DB_URL=postgresql://username:password@host:port/database?sslmode=require
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USERNAME=
DB_PASSWORD=
DB_SSL=true

# JWT
JWT_SECRET=your-strong-random-secret-min-32-chars
JWT_EXPIRY=24h

# Frontend (for CORS and cookies)
FRONTEND_URL=http://localhost:5173

# Node
NODE_ENV=development
